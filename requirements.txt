# Many-Hand-Shop one-command setup (PowerShell)
# Run from project root with this single command:
# Get-Content .\requirements.txt | Where-Object { $_.Trim() -and -not $_.Trim().StartsWith('#') } | ForEach-Object { Invoke-Expression $_ }

node -v
npm -v

npm install

Push-Location frontend
npm install
Pop-Location

npm ls cloudinary multer
